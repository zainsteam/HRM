import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.unionsecurity',
  appName: 'Union Security',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
